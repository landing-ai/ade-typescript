.prism.log
.stdy.log
node_modules
yarn-error.log
codegen.log
Brewfile.lock.json
dist
dist-deno
/*.tgz
.idea/
.eslintcache
dist-bundle
oidc

# api-extractor working dir (surface-lock); the committed report lives in etc/
temp

# local env / secrets — never commit API keys
.env
.env.local
.env.*.local
